---
title: lowbit 与树状数组：数学与位运算精讲
category: 数学与位运算
summary: 树状数组用 lowbit 管理每个节点覆盖的后缀区间，支持前缀和与单点更新。
problem_ids: [307, 315, 493, 327]
order: 207
---

# lowbit 与树状数组：数学与位运算精讲

## 一句话总结

`i & -i` 表示下标 i 负责的区间长度。

## 识别信号

区域和检索、逆序对、翻转对、区间和个数。

对「lowbit 与树状数组」来说，识别信号不是关键词本身，而是它们都在要求你先找到不随操作改变的等式、余数、奇偶或位性质，再把枚举范围压下来。读题时先把样例拆成“已经确认”“仍在候选”“答案落点”三块，再决定用哪种结构推进。

## 等式与位性质锚点

- 本篇围绕「lowbit 与树状数组」训练：`i & -i` 表示下标 i 负责的区间长度。
- 数学题的关键是保持等价变形，任何取模、约分或异或都不能改变答案含义。
- 识别信号里的“区域和检索、逆序对、翻转对、区间和个数”要能和代码里的每一次推进对应起来，否则说明你只记住了模板，没有抓住约束。
- 做完 #307、#315、#493、#327 时，复盘「不变量、等价变形、数值范围」是否还能用同一套解释串起来。

## 推导步骤

1. 离散化值域。
2. update 时 i += lowbit(i)。
3. query 时 i -= lowbit(i)。
4. 用前缀差得区间和。

## 套公式前的边界清单

- 0、1、负数、溢出、取模后再比较、边界质数都要单测。
- 对照「lowbit 与树状数组」标明答案在遍历中何时被写入，避免把中间状态误当最终结论。
- 相关题 #307、#315、#493、#327 只保留一个变化点：输入形态、约束或返回值发生了什么改变？
- 若代码需要第二遍、回溯或压缩状态，说明第一遍保留下来的信息刚好够用。

## 易错点

- 0 下标和 1 下标混用。
- 离散化漏掉查询边界。
- long long 溢出。

## 训练题

建议按这个顺序刷：#307, #315, #493, #327。

完成每题后不要只记录 AC。请补三行复盘：

1. 「lowbit 与树状数组」的关键状态是什么，为什么它不会丢答案？
2. 这道题和前一道训练题相比，只多了哪一个约束？
3. 如果明天忘了代码，你会用哪句话把这个模式重新推出来？
