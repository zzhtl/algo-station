---
title: 旋转数组的有序半边：二分搜索精讲
category: 二分搜索
summary: 旋转数组每次至少有一边有序，判断目标是否落在有序半边即可收缩。
problem_ids: [33, 81, 153, 154]
order: 203
---

# 旋转数组的有序半边：二分搜索精讲

## 一句话总结

虽然整体无序，但 `mid` 两侧至少一侧保留了原数组有序性。

## 识别信号

搜索旋转数组、含重复搜索、寻找最小值。

对「旋转数组的有序半边」来说，识别信号不是关键词本身，而是它们都在要求你把答案空间变成真假分界线，并用一次检查函数判断当前 mid 站在哪一侧。读题时先把样例拆成“已经确认”“仍在候选”“答案落点”三块，再决定用哪种结构推进。

## 单调谓词锚点

- 本篇围绕「旋转数组的有序半边」训练：虽然整体无序，但 `mid` 两侧至少一侧保留了原数组有序性。
- 二分维护的是答案区间，不是数组下标本身；每次收缩都要保留可能答案。
- 识别信号里的“搜索旋转数组、含重复搜索、寻找最小值”要能和代码里的每一次推进对应起来，否则说明你只记住了模板，没有抓住约束。
- 做完 #33、#81、#153、#154 时，复盘「谓词单调性、左右边界、返回值」是否还能用同一套解释串起来。

## 推导步骤

1. 比较 nums[l] 和 nums[mid] 判断左半有序。
2. 目标在有序区间则保留。
3. 否则去另一边。

## 收缩边界前的边界清单

- 最小答案、最大答案、无解、重复值、mid 靠近边界都要验。
- 对照「旋转数组的有序半边」标明答案在遍历中何时被写入，避免把中间状态误当最终结论。
- 相关题 #33、#81、#153、#154 只保留一个变化点：输入形态、约束或返回值发生了什么改变？
- 若代码需要第二遍、回溯或压缩状态，说明第一遍保留下来的信息刚好够用。

## 易错点

- 重复元素导致无法判断时没收缩。
- 边界包含关系错。
- 找最小值和找目标模板混用。

## 训练题

建议按这个顺序刷：#33, #81, #153, #154。

完成每题后不要只记录 AC。请补三行复盘：

1. 「旋转数组的有序半边」的关键状态是什么，为什么它不会丢答案？
2. 这道题和前一道训练题相比，只多了哪一个约束？
3. 如果明天忘了代码，你会用哪句话把这个模式重新推出来？
