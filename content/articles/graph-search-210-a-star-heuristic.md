---
title: 启发式搜索的估价函数：搜索与图论精讲
category: 搜索与图论
summary: A* 在最短路基础上加入不会高估的启发函数，让搜索更快朝目标推进。
problem_ids: [752, 773, 505]
order: 210
---

# 启发式搜索的估价函数：搜索与图论精讲

## 一句话总结

优先级是已走代价 g 加估计剩余代价 h，h 不高估时仍能保证最优。

## 识别信号

滑动谜题、迷宫最短路、转盘锁优化搜索。

对「启发式搜索的估价函数」来说，识别信号不是关键词本身，而是它们都在要求你先把题目里的对象建成状态和边，再决定用 BFS、拓扑、最短路还是连通性工具推进。读题时先把样例拆成“已经确认”“仍在候选”“答案落点”三块，再决定用哪种结构推进。

## 状态建图锚点

- 本篇围绕「启发式搜索的估价函数」训练：优先级是已走代价 g 加估计剩余代价 h，h 不高估时仍能保证最优。
- visited、距离或并查集保存的是图上的事实，不能只按原始节点编号粗略标记。
- 识别信号里的“滑动谜题、迷宫最短路、转盘锁优化搜索”要能和代码里的每一次推进对应起来，否则说明你只记住了模板，没有抓住约束。
- 做完 #752、#773、#505 时，复盘「状态维度、边的语义、访问标记时机」是否还能用同一套解释串起来。

## 推导步骤

1. 定义状态和真实代价。
2. 设计 admissible 启发函数。
3. 按 f=g+h 入堆。
4. 目标出堆时返回。

## 入队松弛前的边界清单

- 孤立点、自环、重边、不可达、起终点相同都要考虑。
- 对照「启发式搜索的估价函数」标明答案在遍历中何时被写入，避免把中间状态误当最终结论。
- 相关题 #752、#773、#505 只保留一个变化点：输入形态、约束或返回值发生了什么改变？
- 若代码需要第二遍、回溯或压缩状态，说明第一遍保留下来的信息刚好够用。

## 易错点

- 启发函数高估导致错过最优。
- visited 时机过早。
- 状态转移代价不一致。

## 训练题

建议按这个顺序刷：#752, #773, #505。

完成每题后不要只记录 AC。请补三行复盘：

1. 「启发式搜索的估价函数」的关键状态是什么，为什么它不会丢答案？
2. 这道题和前一道训练题相比，只多了哪一个约束？
3. 如果明天忘了代码，你会用哪句话把这个模式重新推出来？
