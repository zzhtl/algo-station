---
title: Trie 前缀树计数：设计与数据结构精讲
category: 设计与数据结构
summary: 前缀查询、单词搜索和异或最大化都可以把字符或二进制位组织成 Trie。
problem_ids: [208, 211, 212, 421]
order: 202
---

# Trie 前缀树计数：设计与数据结构精讲

## 一句话总结

共享前缀只存一次，查询时沿前缀路径走即可。

## 识别信号

实现 Trie、添加搜索单词、单词搜索 II、最大异或。

对「Trie 前缀树计数」来说，识别信号不是关键词本身，而是它们都在要求你把每个操作的时间复杂度要求翻译成内部结构之间的同步关系。读题时先把样例拆成“已经确认”“仍在候选”“答案落点”三块，再决定用哪种结构推进。

## 接口语义锚点

- 本篇围绕「Trie 前缀树计数」训练：共享前缀只存一次，查询时沿前缀路径走即可。
- 外部接口看到的是一个结构，内部多个容器必须在每次操作后保持同一份事实。
- 识别信号里的“实现 Trie、添加搜索单词、单词搜索 II、最大异或”要能和代码里的每一次推进对应起来，否则说明你只记住了模板，没有抓住约束。
- 做完 #208、#211、#212、#421 时，复盘「接口承诺、内部同步、复杂度来源」是否还能用同一套解释串起来。

## 推导步骤

1. 节点保存子节点映射和终止标记。
2. 插入逐字符建边。
3. 查询前缀或通配符 DFS。

## 定结构前的边界清单

- 重复插入、删除不存在、容量边界、查询空结构、时间戳相同都要覆盖。
- 对照「Trie 前缀树计数」标明答案在遍历中何时被写入，避免把中间状态误当最终结论。
- 相关题 #208、#211、#212、#421 只保留一个变化点：输入形态、约束或返回值发生了什么改变？
- 若代码需要第二遍、回溯或压缩状态，说明第一遍保留下来的信息刚好够用。

## 易错点

- 终止标记漏掉导致前缀当完整词。
- 通配符递归爆炸不剪枝。
- 二进制 Trie 位数不统一。

## 训练题

建议按这个顺序刷：#208, #211, #212, #421。

完成每题后不要只记录 AC。请补三行复盘：

1. 「Trie 前缀树计数」的关键状态是什么，为什么它不会丢答案？
2. 这道题和前一道训练题相比，只多了哪一个约束？
3. 如果明天忘了代码，你会用哪句话把这个模式重新推出来？
