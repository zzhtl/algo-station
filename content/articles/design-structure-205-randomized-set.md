---
title: 随机结构的数组哈希组合：设计与数据结构精讲
category: 设计与数据结构
summary: 要等概率随机取元素，需要元素紧凑存放在数组里，哈希表负责 O(1) 定位。
problem_ids: [380, 381, 398, 528]
order: 205
---

# 随机结构的数组哈希组合：设计与数据结构精讲

## 一句话总结

随机访问数组下标是 O(1)，删除靠末尾交换保持数组紧凑。

## 识别信号

随机集合、允许重复随机集合、随机数索引、按权重随机。

对「随机结构的数组哈希组合」来说，识别信号不是关键词本身，而是它们都在要求你把每个操作的时间复杂度要求翻译成内部结构之间的同步关系。读题时先把样例拆成“已经确认”“仍在候选”“答案落点”三块，再决定用哪种结构推进。

## 接口语义锚点

- 本篇围绕「随机结构的数组哈希组合」训练：随机访问数组下标是 O(1)，删除靠末尾交换保持数组紧凑。
- 外部接口看到的是一个结构，内部多个容器必须在每次操作后保持同一份事实。
- 识别信号里的“随机集合、允许重复随机集合、随机数索引、按权重随机”要能和代码里的每一次推进对应起来，否则说明你只记住了模板，没有抓住约束。
- 做完 #380、#381、#398、#528 时，复盘「接口承诺、内部同步、复杂度来源」是否还能用同一套解释串起来。

## 推导步骤

1. 数组保存元素。
2. 哈希表保存位置。
3. 删除时末尾交换。
4. 按权重随机用前缀和加二分。

## 定结构前的边界清单

- 重复插入、删除不存在、容量边界、查询空结构、时间戳相同都要覆盖。
- 对照「随机结构的数组哈希组合」标明答案在遍历中何时被写入，避免把中间状态误当最终结论。
- 相关题 #380、#381、#398、#528 只保留一个变化点：输入形态、约束或返回值发生了什么改变？
- 若代码需要第二遍、回溯或压缩状态，说明第一遍保留下来的信息刚好够用。

## 易错点

- 重复元素位置集合没维护。
- 删除后下标表不同步。
- 权重随机边界偏 1。

## 训练题

建议按这个顺序刷：#380, #381, #398, #528。

完成每题后不要只记录 AC。请补三行复盘：

1. 「随机结构的数组哈希组合」的关键状态是什么，为什么它不会丢答案？
2. 这道题和前一道训练题相比，只多了哪一个约束？
3. 如果明天忘了代码，你会用哪句话把这个模式重新推出来？
