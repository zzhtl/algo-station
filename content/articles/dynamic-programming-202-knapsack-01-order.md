---
title: 01 背包倒序枚举容量：动态规划精讲
category: 动态规划
summary: 01 背包每个物品只能用一次，所以一维压缩时容量必须倒序。
problem_ids: [416, 494, 1049, 474]
order: 202
---

# 01 背包倒序枚举容量：动态规划精讲

## 一句话总结

倒序保证当前物品不会在同一轮被重复使用。

## 识别信号

分割等和子集、目标和、最后一块石头、0/1 字符串。

对「01 背包倒序枚举容量」来说，识别信号不是关键词本身，而是它们都在要求你把重复子问题压成状态，并保证每一次转移只从已经定义清楚的旧状态来。读题时先把样例拆成“已经确认”“仍在候选”“答案落点”三块，再决定用哪种结构推进。

## 状态转移锚点

- 本篇围绕「01 背包倒序枚举容量」训练：倒序保证当前物品不会在同一轮被重复使用。
- 状态含义要先于代码确定；如果一句话说不清 dp[i] 表示什么，转移大概率会写散。
- 识别信号里的“分割等和子集、目标和、最后一块石头、0/1 字符串”要能和代码里的每一次推进对应起来，否则说明你只记住了模板，没有抓住约束。
- 做完 #416、#494、#1049、#474 时，复盘「状态定义、转移来源、遍历顺序」是否还能用同一套解释串起来。

## 推导步骤

1. 把题目转成容量和价值。
2. 遍历物品。
3. 容量从大到小更新。
4. 答案看容量是否可达或最大价值。

## 写转移前的边界清单

- 初始状态、不可能状态、容量或下标为 0、答案取 max 还是求和要分清。
- 对照「01 背包倒序枚举容量」标明答案在遍历中何时被写入，避免把中间状态误当最终结论。
- 相关题 #416、#494、#1049、#474 只保留一个变化点：输入形态、约束或返回值发生了什么改变？
- 若代码需要第二遍、回溯或压缩状态，说明第一遍保留下来的信息刚好够用。

## 易错点

- 正序枚举把 01 背包写成完全背包。
- 目标和转化公式没判奇偶。
- 二维状态压缩覆盖。

## 训练题

建议按这个顺序刷：#416, #494, #1049, #474。

完成每题后不要只记录 AC。请补三行复盘：

1. 「01 背包倒序枚举容量」的关键状态是什么，为什么它不会丢答案？
2. 这道题和前一道训练题相比，只多了哪一个约束？
3. 如果明天忘了代码，你会用哪句话把这个模式重新推出来？
